---
import { Icon } from "astro-icon/components";

const { name, icon } = Astro.props;
---

<li class="flex items-center gap-x-2">
    <Icon class="text-2xl text-decorate" name={icon} />
    <span class="text-sm">{name}</span>
</li>
